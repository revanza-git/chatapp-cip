# =============================================================================
# SETUP INSTRUCTIONS:
# =============================================================================
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Update values in .env.local with your actual configuration
# 3. .env.local is automatically loaded and ignored by git
#
# PRIORITY ORDER (highest to lowest):
# 1. .env.local (for local development - ignored by git)  
# 2. .env (for shared defaults - can be committed)
# 3. System environment variables
# =============================================================================

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8080

# Backend Configuration
DATABASE_URL=postgresql://chatbot_user:chatbot_password@postgres:5432/chatbot_db
UPLOADS_DIR=uploads
SKIP_DATABASE=false

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters

# AI Configuration
AI_ENABLED=false  # Set to true to enable AI features (requires Ollama service)
OLLAMA_URL=https://90b90cad6d42.ngrok-free.app  # Update with your ngrok URL
HF_TOKEN=your-hugging-face-token-here

# Server Configuration
PORT=8080
FRONTEND_URL=http://localhost:3000

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update DATABASE_URL for your database
# 3. Change JWT_SECRET to a secure random string (minimum 32 characters)
# 4. For production deployment, update FRONTEND_URL and NEXT_PUBLIC_API_URL accordingly
